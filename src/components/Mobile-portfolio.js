/* function toggleElementVisibility() {
    // Get viewport dimensions
    const viewportWidth = window.innerWidth;

    // Set a condition based on your desired viewport dimensions
    const shouldHideElement = viewportWidth < 800;

    // Get all elements with the class 'description'
    const elementsToToggle = document.querySelectorAll('.description');

    // Log some information for debugging
    console.log('Should hide element:', shouldHideElement);
    console.log('Number of elements:', elementsToToggle.length);

    // Toggle the visibility of each element based on the condition
    elementsToToggle.forEach(element => {
        element.style.display = shouldHideElement ? 'none' : 'block';
    });
}

// Call the function initially and listen for window resize events
toggleElementVisibility();
window.addEventListener('resize', toggleElementVisibility); */
